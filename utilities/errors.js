'use strict';

const notFound = {
    message: 'Not found'
};

const invalid = {
    message: 'Invalid request'
}

const unauthorized = {
    message: 'Unauthorized'
}

module.exports = {
    notFound,
    invalid,
    unauthorized
}